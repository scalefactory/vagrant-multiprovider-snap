en:
  vagrant_snap:
    commands:
      list:
        output: |-
          Current available snapshots by created VM:

          %{snapshots}
      delete:
        output: |-
          Snapshot does not exist:

          %{snapshot}
      rollback:
        output: |-
          Snapshot does not exist:

          %{snapshot}

    actions:
      vm:
        snapshot_take:
          taking: |-
            Taking snapshot.
        snapshot_rollback:
          rolling_back: |-
            Rolling back to previous snapshot
        snapshot_delete:
          deleting: |-
            Deleting snapshot
        snapshot_not_created: |-
          Snapshot not created for this VM
        snapshot_not_deleted: |-
          Snapshot not deleted for this VM

