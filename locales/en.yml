en:
  vagrant_snap:
    commands:
      list:
        output: |-
          Current available snapshots by created VM:

          %{snapshots}

    actions:
      vm:
        snapshot_take:
          taking: |-
            Taking snapshot.
          taking_named: |-
            Taking snapshot - %{snapshot}
        snapshot_rollback:
          rolling_back: |-
            Rolling back to snapshot - %{snapshot}
          not_exist: |-
            Snapshot - %{snapshot} - does not exist
        snapshot_delete:
          deleting: |-
            Deleting snapshot - %{snapshot}
          not_exist: |-
            Snapshot - %{snapshot} - does not exist
        snapshot_not_created:
          not_created: |-
            Snapshot not created for this VM
          not_exist: |-
            Snapshot - %{snapshot} - does not exist
        snapshot_not_deleted:
          not_deleted: |-
            Snapshot - %{snapshot} - not deleted for this VM

